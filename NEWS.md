# AnVILBase 1.6.0

* Use `GCPtools::gcloud_exists()` for checking `gcloud` availability.

# AnVILBase 1.4.0

* Added R version dependency (>= 4.4.0).

# AnVILBase 1.2.0

* No changes.

# AnVILBase 1.0.0

* Added `has_avworkspace` generic and methods to check for the presence of an
  AnVIL workspace.
* Added `avworkspace*` and `tables*` generics and methods.
* Remove `avdata` generics and methods (not supported on Azure).

# AnVILBase 0.99.0

* Initial Bioconductor submission.
